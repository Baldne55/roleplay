{
  "name": "roleplay",
  "version": "0.5.0",
  "private": true,
  "description": "Text-roleplay FiveM server (Shared + Backend + Frontend + UI + LoadScreen).",
  "workspaces": [
    "Shared",
    "Backend",
    "Frontend",
    "UI",
    "LoadScreen"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "eslint-plugin-vue": "^9.32.0",
    "globals": "^14.0.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
